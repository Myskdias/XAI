# XAI BERT

- BERT hidden-state extraction
- token flattening and layer means
- outlier dimension analysis and clipping
- anisotropy measurement before/after clipping
- linear position probe training and contribution analysis

## Structure

- `projet.ipynb`: original notebook (unchanged)
- `src/xai_bert/`: reusable modules
- `scripts/`: runnable examples matching notebook experiments

## Install

```bash
pip install -e .
```

